# Part 1
<ol>
  <li>values added: 20</li>
  <li>values added: 20</li>
  <li>Using let to create variables has block scope which makes it similar to other popular programming languages including Java, C++, and C making it easier to use.</li>
  <li>values added: 20</li>
  <li>There is an error because the variable result was not defined in that code block so it is out of scope.</li>
  <li>There is an error because the program tries to assign a value to a const variable after it was declared.</li>
  <li>There is an error because the variable result was not defined in that code block so it is out of scope.</li>
</ol>
